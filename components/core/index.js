var ui = require('./ui');
var { plugin, widget } = require('./plugin');
var Component = require('./Component');
var position = require('./position');

module.exports = {
  UI: ui,
  Plugin: plugin,
  Widget: widget,
  Component: Component,
  position: position
};
